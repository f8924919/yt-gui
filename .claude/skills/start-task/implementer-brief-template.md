# 実装ブリーフの雛形

`implementer` へ実装を委譲するときは、**このファイルを `.brief/<slug>.md` へ複写して埋め**、起動プロンプトには
**パスと「このファイルを読め・文脈を失ったと感じたら読み直せ」だけ**を書く（取り決めの正本は
[git-workflow.md](../../../docs/git-workflow.md) §5.2「実装の委譲」）。

`.brief/` は `.gitignore` 済みで、追跡されない。**scratchpad には置かない**
（セッション固有なので分割点 A / B で `/clear` した瞬間に消える）。

---

## 背景（1〜3 行）

<!-- 何のタスクか。Issue 番号と、このブリーフが担当する範囲。 -->

## 触ってよいファイル

<!-- パスを列挙する。ここに無いファイルは触らせない。
     並行する実装担当がいる場合は「相手が同じ木で作業している」と 1 行書く。
     長いジョブ（ビルド等）が読んでいるファイルを含めない。 -->

## やること

<!-- 項目ごとに、変更内容と対象 path。設計は済んでいる前提で、選択肢を残さない。 -->

## 判定と期待値

<!-- ケース表・期待値・変異と死因の対応まで**主エージェントが**書く。
     ここが埋まっていないなら、まだ委譲してよい段階ではない。 -->

## 触ってはいけないもの

<!-- 設計の変更・受け入れ条件の変更・commit / push。 -->

## 報告してほしいこと

<!-- 既定は agents/implementer.md の報告フォーマット。追加で見たいものがあればここに書く。
     例: 検査を外したときの実際の例外、before / after の件数とその数え方。 -->

---

## 追記（差し戻し・追加指示）

<!-- 差し戻しは SendMessage の本文だけで送らず、ここへ追記する（本文は auto-compact で消える）。
     日付つきで追記していく。 -->
