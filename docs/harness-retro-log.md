# ハーネス見直しの記録

[git-workflow.md](git-workflow.md) §5.9 の見直し（`/harness-retro`）を 1 回 1 節で残す。**ルールは §5.9 が正本**で、ここには回ごとの窓・候補表・採否・メモリ整理の前後だけを書く。**新しい回を上に足す**。次回の窓の始点は、「窓:」の行を持つ節のうち、いちばん上の節の「終点」（メモリ整理だけの節は窓を持たないので数えない。§5.9「窓」）。

**記録の無いものは「記録なし」と書く**（推測で埋めない。他の docs・メモリへ要約するときも言い換えない）。

<!-- 節の雛形:
## YYYY-MM-DD（起票 #a〜#b）

- 窓: 始点 YYYY-MM-DD `<hash>` → 終点 YYYY-MM-DD `<hash>`（extract 時点の origin/main）
- 配分（窓内）: archive 行 ハーネス系 N / 本業系 M・コミット N
- ユーザー判断: 起票本数・起票順・本業の作業との前後

| # | 候補（繰り返した失敗の型） | 実害の実例 | 置き場所 | 採否と理由 | Issue |
|---|---|---|---|---|---|

- follow-up（実装中に派生して起票したもの）:
- メモリ整理（YYYY-MM-DD・<マシン名>）: N → M ファイル（MEMORY.md を除く）。削除: … / 統合: … / 縮めた: …
-->

（まだ見直しを行っていません）
